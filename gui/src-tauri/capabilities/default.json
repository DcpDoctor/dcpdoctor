{
  "identifier": "default",
  "description": "Default capabilities for dcpdoctor",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "dialog:allow-open",
    "core:window:allow-set-title",
    "core:window:default"
  ]
}
